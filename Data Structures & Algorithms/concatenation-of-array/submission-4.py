class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        ne_list =[]
        for i in range(len(nums)):
            ne_list.append(nums[i])
        for i in range(len(nums)):
            ne_list.append(nums[i])
        return ne_list 
        